# 生成タイルの配置先（生成物はGit管理対象外）

ビルドパイプラインが生成したPMTilesファイル（路線用・地点用）をこのディレクトリに
配置する。`*.pmtiles` はリポジトリの `.gitignore` で除外される。

- OpenSpec Change: `highway-facility-map`
- tasks.md タスク番号: 3.4（GitHub Issue #3）
