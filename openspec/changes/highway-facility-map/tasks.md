## 1. プロジェクトのセットアップ（Issue: #1）

- [x] 1.1 タイル生成用の作業ディレクトリ構成（前処理スクリプト、tippecanoe実行
      設定、出力先）を作成する
- [x] 1.2 tippecanoeとPMTiles CLI（もしくはPMTiles出力に対応したtippecanoe）を
      ビルド環境に導入する
- [x] 1.3 静的サイトの雛形ディレクトリ（`index.html`、スタイル定義、生成タイル
      配置先）を作成する

## 2. 現況データの前処理（Issue: #2）

- [x] 2.1 `N06-25_HighwaySection.geojson` を読み込み、`N06_003`（設置期間終了年）
      が `9999` の地物のみを抽出するスクリプトを実装する
- [x] 2.2 抽出した路線地物から `N06_007`（路線名）・`N06_008`（路線種別区分）
      以外の不要属性を除去し、タイル用GeoJSONとして書き出す
- [x] 2.3 `N06-25_Joint.geojson` を読み込み、`N06_014`（設置期間終了年）が
      `9999` の地物のみを抽出するスクリプトを実装する
- [x] 2.4 抽出した地点地物に、`N06_019`（接合部種別）に基づく重要度ティアと
      tippecanoe用`minzoom`（JCT=8 / 一般IC=10 / スマートIC=12 / その他=14）を
      `tippecanoe` プロパティとして付与し、タイル用GeoJSONとして書き出す
- [x] 2.5 前処理スクリプトの入出力件数（現況路線1,289件・現況地点2,384件、
      種別内訳）を検証するチェックを追加する

## 3. MVT（PMTiles）タイル生成パイプライン（Issue: #3）

- [x] 3.1 路線用GeoJSONから `tippecanoe -Z4 -z14` で路線レイヤーのタイルを生成
      するビルドコマンド/スクリプトを実装する
- [x] 3.2 地点用GeoJSONから `tippecanoe -Z8 -z14` ＋地物単位minzoom＋密度ベース
      間引き（`--drop-densest-as-needed` 等）で地点レイヤーのタイルを生成する
      ビルドコマンド/スクリプトを実装する。z14相当は間引き対象から除外する
- [x] 3.3 tippecanoeの出力（mbtiles等）をPMTiles形式に変換する、またはPMTiles
      を直接出力するステップを実装する
- [x] 3.4 生成された路線用・地点用PMTilesを静的サイトの出力ディレクトリに配置
      するステップを実装する
- [x] 3.5 一連の前処理→タイル生成→配置を1コマンドで実行できるビルドスクリプト
      にまとめる

## 4. タイル生成結果の検証（Issue: #3）

- [x] 4.1 z8タイル群をデコードし、収録される地点種別がジャンクションのみで
      あることを確認するチェックスクリプトを作成する
- [x] 4.2 z10/z12タイル群をデコードし、それぞれ一般IC/スマートICが段階的に
      追加され、より低いズームの収録集合を包含していることを確認する
- [x] 4.3 z14タイル群をデコードし、地物数の合計が現況地点数（2,384件）と一致
      すること（間引きが発生していないこと）を確認する
- [x] 4.4 路線タイル群をデコードし、ズーム4〜14の各レベルでタイルが生成されて
      おり、路線名・路線種別区分属性が保持されていることを確認する

## 5. MapLibre GL JS 地図サイトの基本実装（Issue: #4）

- [x] 5.1 MapLibre GL JS本体とPMTiles protocolプラグインを静的サイトに読み込み
      構成する（CDN配信のビルド済みバンドルを利用）
- [x] 5.2 PMTiles protocolを登録し、路線用・地点用PMTilesをベクトルタイル
      ソースとして追加するMapLibreスタイル定義（背景レイヤーなし）を実装する
- [x] 5.3 地図の初期表示範囲・初期ズームなど基本的な地図初期化コードを実装する

## 6. 路線スタイルの実装（Issue: #5）

- [ ] 6.1 国土地理院の最適化ベクトルタイル（標準地図）の道路表現を参考に、
      ケーシング用・フィル用の`line`レイヤーをそれぞれ実装する
- [ ] 6.2 `line-width`・`line-color`をズームレベルに応じて変化させる
      `interpolate`式を実装する
- [ ] 6.3 `N06_008`（路線種別区分）の値に応じた配色・線幅のバリエーションを
      `match`/`case`式で実装する
- [ ] 6.4 `symbol-placement: "line"`の`symbol`レイヤーで路線名
      （`N06_007`）をライン沿いに表示し、`text-allow-overlap: false`等で
      ラベルの重なりを回避する

## 7. 地点スタイルの実装（Issue: #6）

- [ ] 7.1 Googleマップのピン/POIマーカー表現を参考に、`N06_019`（接合部種別）
      に応じた配色・形状・サイズを`match`式で分岐する地点マーカーレイヤーを
      実装する
- [ ] 7.2 地点名（`N06_018`）を表示する`symbol`レイヤーを実装する（タイル側
      のminzoom制御により、重要度順の段階的表示がそのまま実現されることを
      確認する）
- [ ] 7.3 マーカーとラベルが重ならないよう、`text-offset`等でレイアウトを調整
      する

## 8. スタイル・表示の確認（Issue: #7）

- [ ] 8.1 開発用の軽量な静的サーバーでサイトを起動し、z4〜z14の各ズームで
      路線・地点の表示を目視確認する
- [ ] 8.2 首都圏・関西圏などJCT/ICが密集するエリアで、低ズーム時の間引き・
      ラベル重なり回避が期待通り機能していることを確認する
- [ ] 8.3 路線種別区分・接合部種別ごとのスタイル差異が視覚的に判別できることを
      確認する

## 9. 配布物の最終化（Issue: #8）

- [ ] 9.1 静的サイト一式（HTML/JS/CSS＋PMTilesファイル）を1つの出力ディレクトリ
      にまとめ、追加のビルドステップなしでそのまま静的ホスティングに配置できる
      ことを確認する
- [ ] 9.2 ビルド手順（前処理からタイル生成、サイト出力まで）をREADME等に記載
      する
